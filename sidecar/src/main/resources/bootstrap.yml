server:
  port: 8009

spring:
  application:
    name: node-service

sidecar:
  port: 8008
  health-uri: http://localhost:8008/status

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
